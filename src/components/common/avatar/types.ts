export interface SAvatarProps {
  user: string;
  image?: string;
  size?: string;
}

export interface SAvatarImage {
  image: string;
}

export interface SAvatarSizeProps {
  sm?: string;
  md?: string;
  lg?: string;

}
